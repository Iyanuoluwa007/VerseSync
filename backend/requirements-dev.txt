-r requirements.txt

# Test runner
pytest==8.3.3
pytest-asyncio==0.24.0

# TestClient for the FastAPI app
httpx==0.27.2

# Lint
ruff==0.7.4

# Optional: only needed to regenerate the README screenshots with
#   python scripts/capture_screenshots.py
# Install separately, it downloads a browser:
#   pip install playwright && python -m playwright install chromium
# playwright
